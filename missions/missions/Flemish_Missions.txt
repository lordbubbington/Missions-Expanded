fla_conquest = {

	slot = 1
	generic = no
	ai = yes
	potential = {
		tag = FLA
	}
	has_country_shield = yes

	fla_win_independence = {
		icon = mission_monarch_in_throne_room
		required_missions = { }
		trigger = {
			is_subject = no
			owns_core_province = 90
			is_at_war = no
		}
		effect = {
			wallonia_area = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { owned_by = ROOT }
				}
				add_permanent_claim = ROOT
			}
			1744 = {
				if = {
					limit = {
						NOT = {
							owned_by = ROOT
							is_core = ROOT
						}
					}
					add_permanent_claim = ROOT
				}
			}
			2974 = {
				if = {
					limit = {
						NOT = {
							owned_by = ROOT
							is_core = ROOT
						}
					}
					add_permanent_claim = ROOT
				}
			}
		}
	}
	fla_unify_belgium = {
		icon = mission_unite_home_region
		required_missions = { fla_win_independence }
		provinces_to_highlight = { 
			OR = {
				area = wallonia_area
				province_id = 1744
				province_id = 2974
			}
			NOT = {
				country_or_non_sovereign_subject_holds = ROOT
			}
		}
		trigger = {
			wallonia_area = {
				type = all
				country_or_non_sovereign_subject_holds = ROOT
			}
			1744 = {
				country_or_non_sovereign_subject_holds = ROOT
			}
			2974 = {
				country_or_non_sovereign_subject_holds = ROOT
			}
		}
		effect = {
			add_country_modifier = {
				name = "hegemonic_ambition"
				duration = 9125 #25 years
			}
		}
	}
}

fla_growth = {
		
	slot = 5
	generic = no
	ai = yes
	potential = {
		tag = FLA
	}
	has_country_shield = yes
	
	fla_economic_development = {
		icon = mission_war_chest
		required_missions = { }
		trigger = { treasury = 500 }
		effect = {
			add_country_modifier = {
				name = "strong_treasury"
				duration = 9125
			}
		}
	}
	flemish_renaissance = {
		icon = mission_early_game_buildings
		required_missions = { fla_economic_development }
		trigger = {
			marketplace = 2
			is_institution_enabled = renaissance
			has_institution = renaissance
		}
		effect = {
			add_manpower = 10
			add_dip_power = 100
			add_adm_power = 100
			add_mil_power = 100
			}
		}
	}